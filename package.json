{
  "name": "aws-browse",
  "version": "1.0.0",
  "description": "Command-line interface for AWS S3 bucket management",
  "main": "dist/aws-s3-cli.js",
  "scripts": {
    "build": "tsc",
    "start": "node dist/aws-s3-cli.js",
    "dev": "ts-node src/aws-s3-cli.ts",
    "test": "echo \"Error: no test specified\" && exit 1",
    "lint": "eslint . --ext .ts",
    "prepare": "npm run build"
  },
  "bin": {
    "aws-browse": "./dist/aws-s3-cli.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@aws-sdk/client-s3": "^3.758.0",
    "@aws-sdk/credential-providers": "^3.758.0",
    "dotenv": "^16.4.7",
    "yargs": "^17.7.2"
  },
  "devDependencies": {
    "@types/node": "^22.13.9",
    "@types/yargs": "^17.0.33",
    "ts-node": "^10.9.2",
    "typescript": "^5.8.2"
  },
  "engines": {
    "node": ">=14.0.0"
  },
  "keywords": [
    "aws",
    "s3",
    "cli",
    "bucket",
    "storage",
    "cloud"
  ]
}
